on:
  workflow_dispatch:
  schedule:
    - cron: '45 6,14,22 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t/te1eg2am3@cron
        with:
          token: ${{ secrets.token }}
